#include <limits.h>
#include <stdio.h>
#include <main.h>
int  main(void)
/**
 * main - Entry point
 *
 * Return: 0
*/
{
	char str[] = "_putchar";
	int ch;
	for (ch = 0; ch < 8; ch++)
		_putchar(str[ch]:
	_putchar('\n');

return (0);
}
